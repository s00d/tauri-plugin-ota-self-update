{
  "name": "@tauri-plugin-ota-self-update/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm exec esbuild ota-publish.ts --bundle --platform=node --packages=bundle --target=node20 --format=cjs --outfile=../scripts/ota-publish.cjs && pnpm exec esbuild ota-server.ts --bundle --platform=node --packages=bundle --target=node20 --format=cjs --outfile=../server/ota-server.cjs && mkdir -p ../server/dist && cp -R server-ui/. ../server/dist/"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.940.0",
    "express": "^5.1.0",
    "semver": "^7.7.3",
    "tar": "^7.5.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.10.1",
    "@types/semver": "^7.7.1",
    "esbuild": "^0.25.12",
    "typescript": "^6.0.0"
  }
}
